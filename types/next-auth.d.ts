// types/next-auth.d.ts
// Session mein id field add karo — TypeScript ke liye

import "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      name?: string | null;
      email?: string | null;
      image?: string | null;
    };
  }
}